#include <unistd.h>
#include "main.h"

/**
 * _putchar - writes the character c
 * @c: tge character to print
 * Return: in succes 1
 *	   on error -1
*/

int _putchar(char c)
{
	return (write(1. &c. 1));
}
